<?xml version="1.0" encoding="utf-8"?>
<Package
    xmlns="http://schemas.microsoft.com/appx/manifest/foundation/windows10"
    xmlns:uap="http://schemas.microsoft.com/appx/manifest/uap/windows10"
    xmlns:rescap="http://schemas.microsoft.com/appx/manifest/foundation/windows10/restrictedcapabilities"
    IgnorableNamespaces="uap rescap">

    <Identity
        Name="{{identityName}}"
        Publisher="{{publisher}}"
        Version="{{version}}"
        ProcessorArchitecture="{{processorArchitecture}}"/>

    <Properties>
        <DisplayName>{{displayName}}</DisplayName>
        <PublisherDisplayName>{{publisherDisplayName}}</PublisherDisplayName>
        <Logo>resources/icon_50.png</Logo>
    </Properties>

    <Dependencies>
        <TargetDeviceFamily
            Name="{{targetDeviceFamilyName}}"
            MinVersion="{{targetDeviceFamilyMinVersion}}"
            MaxVersionTested="{{targetDeviceFamilyMaxVersionTested}}"/>
    </Dependencies>

    <Resources>
        <Resource Language="en-us"/>
    </Resources>

    <Applications>
        <Application
            Id="{{appId}}"
            Executable="{{appExecutable}}"
            EntryPoint="Windows.FullTrustApplication">
            <uap:VisualElements
                DisplayName="{{displayName}}"
                Description="{{description}}"
                BackgroundColor="{{backgroundColor}}"
                Square150x150Logo="resources/icon_150.png"
                Square44x44Logo="resources/icon_44.png">
            </uap:VisualElements>
        </Application>
    </Applications>

    <Capabilities>
        <rescap:Capability Name="runFullTrust"/>
    </Capabilities>
</Package>
